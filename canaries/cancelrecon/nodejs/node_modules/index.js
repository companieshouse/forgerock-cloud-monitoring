var synthetics = require('Synthetics');
const log = require('SyntheticsLogger');

const cancelRecon = async function () {

    let accessToken;
    const { FIDC_URL, USER, PASSWORD, ADMIN_CLIENT, ADMIN_CLIENT_SECRET, MONITORED_COMPONENT, RECON_DURATION, CANCEL_RECON_AFTER } = process.env

    const validateAccessToken = async function (res) {
        return new Promise((resolve, reject) => {
            if (res.statusCode < 200 || res.statusCode > 299) {
                throw res.statusCode + ' ' + res.statusMessage;
            }

            let responseBody = '';
            res.on('data', (d) => {
                responseBody += d;
            });

            res.on('end', () => {
                // Add validation on 'responseBody' here if required.
                accessToken = JSON.parse(responseBody).access_token;
                resolve();
            });
        });
    };

    const validateReconDuration = async function (res) {
        return new Promise((resolve, reject) => {
            if (res.statusCode < 200 || res.statusCode > 299) {
                throw res.statusCode + ' ' + res.statusMessage;
            }

            let responseBody = '';
            res.on('data', (d) => {
                responseBody += d;
            });

            res.on('end', async () => {
                const mappingsWithRecons = JSON.parse(responseBody).mappings.filter(mapping => mapping.recon)

                await Promise.all(mappingsWithRecons.map(async (mappingWithRecon) => {
                    // if (mappingWithRecon && mappingWithRecon.recon.state === 'ACTIVE' && mappingWithRecon.recon.duration > `${CANCEL_RECON_AFTER}`) {
                        if (mappingWithRecon && mappingWithRecon.recon.state === 'SUCCESS') {
                        log.info(`MappingsName: ${mappingWithRecon.recon.mapping} recon has been cancelled`)
                        // Cancel Mappings Recon
                        let cancelRecon = {
                            hostname: FIDC_URL.replace('https://', ''),
                            method: 'GET',
                            path: `/openidm/endpoint/mappingDetails?mapping=${mappingWithRecon.recon.mapping}`,
                            port: '443',
                            protocol: 'https:',
                            body: "",
                            headers: { "Content-Type": "application/json", "Authorization": `Bearer ${accessToken}` }
                        };
                        cancelRecon['headers']['User-Agent'] = [synthetics.getCanaryUserAgentString(), cancelRecon['headers']['User-Agent']].join(' ');

                        // Mark Mappings Recon as cancelled
                        let markAsCancelled = {
                            hostname: FIDC_URL.replace('https://', ''),
                            method: 'GET',
                            path: `/openidm/endpoint/mappingDetails?mapping=${mappingWithRecon.recon.mapping}`,
                            port: '443',
                            protocol: 'https:',
                            body: "",
                            headers: { "Content-Type": "application/json", "Authorization": `Bearer ${accessToken}` }
                        };
                        markAsCancelled['headers']['User-Agent'] = [synthetics.getCanaryUserAgentString(), markAsCancelled['headers']['User-Agent']].join(' ');
                    
                        // Set step config option for Cancel Mappings Recon
                        let configReconDuration = {
                            includeRequestHeaders: false,
                            includeResponseHeaders: false,
                            includeRequestBody: false,
                            includeResponseBody: false,
                            restrictedHeaders: [],
                            continueOnHttpStepFailure: true
                        };
                        return await synthetics.executeHttpStep(`Verify Mapping ${mappingWithRecon.name}`, validateReconDuration, configReconDuration);
                    }
                }))

                resolve(`finished`)
            });
        });
    };

    // Set request option for Get Access Token
    const pw = `${PASSWORD}`
    const encodedPassword = encodeURIComponent(pw)
    const clientSecret = `${ADMIN_CLIENT_SECRET}`
    const encodedClientSecret = encodeURIComponent(clientSecret)

    let requestAccessToken = {
        hostname: FIDC_URL.replace('https://', ''),
        method: 'POST',
        path: '/am/oauth2/realms/root/realms/alpha/access_token?auth_chain=PasswordGrant',
        port: '443',
        protocol: 'https:',
        body: `grant_type=password&username=${USER}&password=${encodedPassword}&scope=fr:idm:*&client_id=${ADMIN_CLIENT}&client_secret=${encodedClientSecret}`,
        headers: { "Content-Type": "application/x-www-form-urlencoded" }
    };
    requestAccessToken['headers']['User-Agent'] = [synthetics.getCanaryUserAgentString(), requestAccessToken['headers']['User-Agent']].join(' ');

    // Set step config option for Get Access Token
    let configAccessToken = {
        includeRequestHeaders: false,
        includeResponseHeaders: false,
        includeRequestBody: false,
        includeResponseBody: false,
        restrictedHeaders: [],
        continueOnHttpStepFailure: true
    };

    await synthetics.executeHttpStep('Get Access Token', requestAccessToken, validateAccessToken, configAccessToken);

    const mappings = MONITORED_COMPONENT.split(',');

    await Promise.all(mappings.map(async (mapping) => {
      // Set request option for Verify Mappings Recon
      let requestReconDuration = {
        hostname: FIDC_URL.replace('https://', ''),
        method: 'GET',
        path: `/openidm/endpoint/mappingDetails?mapping=${mapping}`,
        port: '443',
        protocol: 'https:',
        body: "",
        headers: { "Content-Type": "application/json", "Authorization": `Bearer ${accessToken}` }
      };
      requestReconDuration['headers']['User-Agent'] = [synthetics.getCanaryUserAgentString(), requestReconDuration['headers']['User-Agent']].join(' ');

      // Set step config option for Verify Mappings Recon
      let configReconDuration = {
        includeRequestHeaders: false,
        includeResponseHeaders: false,
        includeRequestBody: false,
        includeResponseBody: false,
        restrictedHeaders: [],
        continueOnHttpStepFailure: true
      };

      return await synthetics.executeHttpStep(`Verify Mapping ${mapping}`, requestReconDuration, validateReconDuration, configReconDuration);
    }))

};

exports.handler = async () => {
    return await cancelRecon();
};

